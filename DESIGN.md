# DESIGN.md — Agent Matrix 工作台视觉系统（唯一事实源）

> 方向：企业控制台 · 蓝白清爽 · 顶栏导航 + KPI 统计卡 + 卡片容器
> 本文档取代 2026-08-21 的「信号编辑室」版（v0.13.0），自 v0.14.0 起生效。
> v0.14.1 修正：恢复全幅流体硬规则——内容区吃满屏宽（此前误锁 1200px 居中窄容器）。
> v0.14.2 修正：语义元素分家——圆点专属「连接活性」，执行状态改用浅底文字徽章；
> 指派选择器改自适应网格卡；下线等危险操作收进 ⋯ 菜单。
> v0.15.0：详情抽屉并行 Agent 结果 tab 化；md/html 结果富文本预览（marked + DOMPurify）。
> 改版原因：编辑室/报刊气质过强（衬线刊头、hairline 分组、无卡片直角），
> 用户反馈「太素、像艺术展/个人博客，不像后台管理系统」，遂对齐主流后管
> （Ant Design Pro 顶栏模式）的结构与组件语言。
> 功能契约（任何样式不豁免）：这是**工作台**——任务状态一眼可辨，
> 3 秒内能开始「新建任务 / 看结果 / 追加指令」。

## 1. 视觉主题与氛围

主流企业后管控制台：顶部导航栏 + 统计卡 + 白色面板卡片，灰底托白卡。
冷灰底 `#F5F7FA` 让白卡片自然浮起；唯一的彩色纪律仍是「品牌蓝给交互、状态色给语义」。
毛玻璃只出现在真正浮起的地方：sticky 顶栏、抽屉、composer。

## 2. 色板与角色

### 基底（冷调，禁暖灰）
- 灰底 `--paper` `#F5F7FA`；卡片/面板 `--surface` `#FFFFFF`
- 墨 `--ink` `#1B2430`；次墨 `--ink-2` `#4A5464`；弱化 `--muted` `#7C8794`
- hairline `--line` `rgba(27,36,48,.09)`；强线 `--line-strong` `rgba(27,36,48,.18)`

### 品牌蓝（唯一交互重音，饱和度受控）
- `--blue` `#1D6FE0`：主按钮、选中导航/tab 下划线、链接、focus 环、::selection
- hover 亮档 `--blue-hi` `#4A8DF0`；柔底 `--blue-soft` `#E8F0FD`
- 蓝不参与状态语义，禁大面积铺色

### 状态语义色（白底对比 ≥4.5:1，语义固定全站一致）
| 角色 | 墨色 | 点/条色 | 用途 |
|---|---|---|---|
| 进行中 amber | `#9A6206` | `#D4940F` | 进行中 / 执行中 |
| 完成 green | `#0B6E4F` | `#16997B` | 完成 / 在线 |
| 失败 rose | `#B62A44` | `#D44D66` | 失败 / 危险操作 |
| 中性 slate | `#5B6676` | `#98A2B0` | 取消 / 离线 |

### 毛玻璃材质（仅浮层）
- sticky 顶栏：`rgba(245,247,250,.82)` + `backdrop-filter: blur(16px) saturate(1.5)`
- 抽屉：`rgba(251,252,254,.88)` + `blur(24px) saturate(1.4)`；遮罩 `rgba(27,36,48,.30)`
- `prefers-reduced-transparency` / 不支持 backdrop-filter 时降级为不透明 `#FFFFFF`

### 铁律
- 禁纯黑 #000、禁紫禁粉、禁渐变光晕、禁大面积蓝色铺底
- 状态色只从上表取；蓝色只给交互；文本对比度 ≥ 4.5:1
- **语义元素纪律**：圆点专属「连接活性」（Agent 在线/离线、SSE 同步）；
  执行状态一律用浅底圆角文字徽章，禁用圆点表达执行状态

## 3. 排版规则

- 全站 sans：系统栈 `-apple-system,"PingFang SC","Hiragino Sans GB","Microsoft YaHei","Noto Sans SC",sans-serif`
  （衬线 Fraunces 自 v0.14.0 起退役，字体文件保留但不再引用）
- 数据 / ID / 时间 / 计数：`JetBrains Mono`（内嵌可变 woff2）+ `tabular-nums`
- 字重只用 400 / 500 / 600；层级靠字重 + 颜色 + 字号三件套，禁斜体
- 字阶：页面/面板标题 15px/600 · 任务标题 15px/600 · 正文 15px/行高 1.65 ·
  meta 12.5px · KPI 大数字 26px/600（mono）· 分组眉 13px/600
- 中文禁负字距；中西文之间加空格；标题 `text-wrap:balance`，正文 `text-wrap:pretty`

## 4. 组件样式

- 圆角刻度：卡片/面板 8px（`--r-card`）· 按钮/输入/标签 6px（`--r-ctl`）· 小标签 4px（`--r-tag`）· 状态点圆形
- 卡片：白底 + 1px `--line` 边 + 一层轻影 `0 1px 2px rgba(16,24,40,.05)`，禁厚重投影堆砌
- KPI 统计卡：5 张 grid 等分（累计任务 / 进行中 / 已完成 / 失败·取消 / 在线 Agent），
  上行 12.5px 标签，下行 mono 26px 大数字（状态类数字用语义色着色）
- 面板卡：卡头（15px/600 标题 + 右侧状态过滤 tab）+ 卡体；过滤 tab 下划线式，选中蓝字蓝线
- 任务行：sans 15px/600 标题 + meta 行（@agent · 轮次 · mono 相对时间，卡住的 Agent 附 amber「卡住」字样）
  + 右侧执行状态徽章与 mono 时间；行间 1px hairline；负 margin 撑满卡宽，整行可点，hover 行底微染 `--blue-soft` 40%
- 分组眉：sans 13px/600（今天 / 昨天 / 更早）+ mono 11px 计数
- 主按钮：`--blue` 白字 6px 圆角；次按钮：白底 1px 灰边；危险：rose 白底描边
- 状态徽章：执行状态用浅底圆角文字 tag（amber/green/rose/gray 四语义）；元信息标签（技能等）用浅灰底 4px 圆角 tag
- 抽屉：右侧滑出，毛玻璃，左缘 1px 边；宽度 520px（新建/详情 720px），移动全宽
- 指派选择器：自适应网格卡（auto-fill minmax 180px），选中蓝边浅蓝底，卡内圆点=在线
- 任务详情：会话线程——纵向 = 轮次（时间），横向 = 并行 Agent 结果 tab（tab 带在线点 + 状态徽章 +
  卡住警示），不再纵向堆叠多份长结果；指令块左缘 2px 墨线引文式；
  结果框头部带「预览 / 原文」切换与复制：md/html 结果经 marked 渲染 + DOMPurify 消毒，
  纯文本只给原文；结果区自然高度，抽屉统一滚动（禁嵌套小滚动区）；
  追加 composer 固定抽屉底部，顶边 hairline + 毛玻璃
- 空态：sans 大句 + 一句指导 + 主按钮；引导步骤用浅灰小卡；骨架屏行 shimmer；toast 墨底白字 6px 圆角

## 5. 布局原则

- 顶栏（sticky 毛玻璃，56px）：左 logo + 产品名（sans 15px/600）+ 主导航（任务 / Agent / 设置）；
  右 同步状态点 + 主操作（任务视图=新建任务，Agent 视图=接入新 Agent）+ 退出
- 内容区：**全幅流体（硬规则）**——吃满屏宽，两侧 24px（移动 16px），禁居中窄容器
- 任务视图：KPI 统计卡行（仅任务视图）+ 任务列表面板卡，任务按时间倒排分「今天 / 昨天 / 更早」
- Agent 视图：面板卡承载行式名单：在线点 + 名称 + 浅灰 tag（技能）+ 最近任务（执行状态徽章）+ mono 心跳；
  危险操作（下线）收进行尾 ⋯ 菜单，不常驻看板
- 设置是主导航第三项的独立视图（不再是抽屉）：全宽面板 + 左标签说明 / 右控件的设置行（控件限宽 460px），附运行信息区
- 登录 / 初始化：居中 420px 白卡片（8px 圆角 + 轻投影）
- 组内间距 < 组间间距；8px 基准刻度

## 6. 深度层级

z 轴：内容 1 · sticky 顶栏 30 · 遮罩 40 · 抽屉 50 · toast 90。
深度语义：卡片靠 1px 边 + 轻影；浮起的东西（顶栏/抽屉/toast）才用毛玻璃。
禁厚重黑影；抽屉只许一层轻影 `0 24px 56px -24px rgba(27,36,48,.25)`。

## 7. Do's / Don'ts

Do：灰底托白卡；KPI 统计卡；mono 数字；状态点+字徽章；毛玻璃只给浮层；行 hover 有反馈
Don't：衬线刊头；斜体；emoji 图标（inline SVG）；蓝色当状态色；圆点表达执行状态；
危险操作常驻看板；厚重投影；任何"AI 蓝紫渐变"

## 8. 响应式

- ≤1080px：统计卡 3 列；≤767px：统计卡 2 列
- ≤767px：顶栏折行（品牌 + 操作一行，导航整行横滚）；任务行隐藏绝对时间保留相对时间；
  抽屉移动全宽；flex 文本子项 `min-width:0`

## 9. Motion 哲学（有反馈，不表演）

一切 UI 反馈 ≤300ms，只动 transform/opacity；ease-out `cubic-bezier(0.23,1,0.32,1)`，
抽屉 `cubic-bezier(0.32,0.72,0,1)`。
- 任务行入场 stagger 40ms（translateY 4px + opacity）
- 签名交互：SSE 推送到达时对应任务行「更新脉冲」——蓝色 ring 呼吸两次后落定
- 进行中状态点 1.6s 呼吸；按压 `:active` 120ms 反馈
- toast 下弹 220ms；抽屉 260ms；键盘高频操作零动画
- hover 包 `@media (hover:hover) and (pointer:fine)`；`prefers-reduced-motion` 去位移留淡入

---

## 工艺密度清单（交付时逐项打勾）

1. 毛玻璃 sticky 顶栏与抽屉（材质细节，用户点名）
2. ::selection 蓝底白字
3. 品牌蓝 focus 环 `:focus-visible`，禁浏览器默认蓝
4. 签名交互：SSE 任务行更新脉冲
5. 自定义细滚动条（冷灰 thumb）
6. 页脚 mono 小字实时同步状态（收尾彩蛋）
7. KPI 统计卡 mono 大数字 + 语义色数字

## 字体与版权

`web/fonts/`：JetBrains Mono（OFL-1.1）随软件再分发；Fraunces（OFL-1.1）文件保留但不再引用，
如需瘦身可连同 `@font-face` 一并删除。中文走系统黑体栈，零下载零版权风险。
`web/vendor/`：Vue 3（MIT）、marked（MIT，md 渲染）、DOMPurify（Apache-2.0 OR MPL-2.0，消毒），
许可文本随文件同目录（`*.LICENSE.txt`）。全部内嵌二进制，运行时零外部请求。
